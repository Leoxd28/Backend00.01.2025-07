/*const today = new Date();
const dayNumber = today.getDay();
console.log(today);
console.log(dayNumber);

switch(dayNumber){
    case 0:
        console.log('Lunes')
        break;
    case 1:
        console.log('Martes')
        break;
    case 2:
        console.log('Miercoles')
        break;
    case 3:
        console.log('Jueves')
        break;
    case 4:
        console.log('Viernes')
        break;
    case 5:
        console.log('Sabado')
        break;
    case 6:
        console.log('Domingo')
        break;
}
let edad = 18
console.log(edad>=18?'si puede votar':'no puede')

let dage ="cuarenta y uno"
age = Number(dage);
if(isNaN(age)){
    voteable ='input is not a number'
    console.log(voteable)
}else{
    voteable = (age >18 ? 'too young':'old enough')
    console.log(voteable)
}

for (let index = 0; index < 10; index++) {
    console.log(index)
    
}
arrDatos = [1,2,3,4,5,3,2,1,7]

for (let index = 0; index < arrDatos.length; index++) {
    const element = arrDatos[index];
    console.log(element);
    
}

arrDatos.forEach(element => {
    console.log(element);
});

//arrDatos.map(console.log(e));

let bandera = true;
let indice = 0;

while (bandera){
    console.log(indice);
    indice++
    if(indice===10){
        bandera = false
    }
}

function sumar(){
    console.log('voy a sumar')
}
sumar();

function isPalindromo(frase){
    const limpio = frase.toLowerCase().replace(/[^a-z0-9]/g, '');
    return limpio === limpio.split('').reverse().join('');
    
}

if(isPalindromo('leonoel')){
    alert('si')
}else{
    alert('no')
}*/

function isPrimo(n){
    if(n<=1)
}